/**
 * @enum ProjectState
 * @description The state of the project
 * @property WAITING_TESTER - The project is waiting for a tester
 * @property IN_PROGRESS - The project is in progress
 * @property COMPLETED - The project is completed
 */
export enum ProjectState {
  WAITING_TESTER = 1,
  IN_PROGRESS = 2,
  COMPLETED = 3,
}
